# Local development only. Do NOT commit a real .env.
# In AWS these come from Secrets Manager via the ECS task definition.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=changeme_local_only
